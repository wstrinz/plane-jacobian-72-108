# Frozen exact versions used to run and verify this repository.
# Captured 2026-07-23 on the reference machine (see ENVIRONMENT.md).
# Install with:  python -m pip install -r requirements-lock.txt
numpy==1.26.4
scipy==1.10.1
sympy==1.14.0
mpmath==1.3.0
python-flint==0.9.0
