# ALT_FRONTIER_V2 -- the alternate regime (`a_t >= 11`) recompiled (machine-generated)

> **DO NOT HAND-EDIT.** Regenerated by `python -u alt_rebuild.py`. Every figure below is computed from the committed artifacts named in section 9; none is typed in. `python -u alt_rebuild.py --quiet` re-derives the whole census and exits nonzero on any drift or on a moved input.

Provenance: git `8d7938c0cd6f2618b5ac7a94bf9cd5233b284435` | schema 1 | 2026-07-25

---

## 0. Headline

> **6 of the 52 alternate-regime branches survive. All 6 are T1.**
>
> `a12_b0000_T1`, `a12_b1000_T1`, `a12_b1100_T1`, `a12_b1110_T1`, `a14_b0000_T1`, `a14_b1000_T1`
>
> The committed **27 open branches (13 T1 + 14 T2)** figure (`ALT_REGIME.md`, `ALT_REGIME_L2.md`, `ALT_INF_SWEEP.md`, `FRONTIER.md`) is **superseded**. The T2 half of the alternate regime is **EMPTY**, and every odd `a_t` is **dead**.
>
> State level: the surviving branches carry **562** of the 3102 states the combination layer left, i.e. **2540 states are removed (81.9%)**.

The derivation below does **not** consume the 27-branch intermediate as a premise. It starts from the full **52-branch** universe read out of `split_place_ledger_sub1.json` and applies only lemmas that are branch-free and regime-free. The 27-branch list is loaded solely as a cross-check (it is a subset of the survivors' complement).

---

## 1. The universe, and why it is 52 and not 27

| source | object | count |
|---|---|---:|
| `split_place_ledger_sub1.json` (`a_t >= 11`) | strata | 26 |
| same, x open branches | branches | 52 |
| `ALT_REGIME.md` + `ALT_REGIME_L2.md` residue | branches | 27 (13 T1 + 14 T2) |
| `alt_inf_sweep.json` | degree states | 38360 -> 4690 surviving |
| `alt_combined.json` | states | 4690 -> 3102 remaining |
| `alt_residue_congruences.json` | states | 3102, 0 kills |

The stratum boundary is `a_t + sum(b_i) <= 15`, which is the sub1 cap `deg e <= 15` -- **a premise, not a theorem**. `a_t >= 16` is out of scope for that reason alone; the trichotomy of section 3 by itself permits every even `a` up to 30.

---

## 2. The lemmas integrated, with their exact scope

| # | lemma | scope | checked here |
|---|---|---|---|
| L1 | `e \| Phi` (K-syzygy, residual 0) | both branches, cap-free, regime-free | B1, B2 |
| L2 | `e \| S` (integral closure) | both branches, cap-free, regime-free | B3 |
| L3 | place trichotomy at `y=-1` | both branches, cap-free, **every place** | B4 |
| L4 | `e*R \| Phi`, `R \| e^2`, `R = c*(y+1)^rho` | **T2 ONLY, permanently** | G6, S3 |

### Scope guards asserted by the compiler

* **L4 is T2-only, permanently.** `T1_BRANCH.md` T8 exhibits `e=1, R=0, S=1, T=1/6, d0=1, d1=-1/3, d2=0, Phi=3/2` on `V(G1,G2,G3,G5)` with `R=0, e!=0, d1!=0`, so no power of `e` lies in `I+(R)` and no power of `Phi` in `I+(e*R)`. Check **G6** re-substitutes that point and confirms all four generators vanish. Stage `S3` is structurally unreachable from a T1 branch. The exact T1 replacements `R | e^2*(e + 3*d1*s)` and `R | e^2*(s^2 - d0)` are **not** used to kill anything here.
* **`t^a | R,S,T` is branch-independent** and does transfer -- but in this regime it is the horn the trichotomy *refutes* (section 3), not an input.
* **The SPINE certificate is sub2-only.** Its five families are `n = 10 - a`, `a = 6..10`; every branch here has `a >= 11`. Check **G7**. It is not imported.
* **`POLE_THEOREM.md` Thm 2C is gated on `3a - 2 < v_t(Phi) = 30`**, i.e. `a <= 10`. The gate is FALSE for every `a` in this regime (check **G8**). The ungated trichotomy of `T1_BRANCH.md` sec.1.2 is used instead.
* **D3** (the `deg Phi` count that forces `deg e = 10` in sub2) is **vacuous** in sub1: `2*deg R = 36 > 34 = deg Phi` already at `deg e = 0` (check **B5**). It contributes nothing here.

---

## 3. The trichotomy at `y = -1`, which is where the regime breaks

```
THEOREM (T1_BRANCH.md sec.1.2; both branches, cap-free, every place)
    v(R) >= v(e)      OR      v(Phi) = v(e) + 2*v(R)

At y = -1:  v(e) = a  and  v(Phi) = 30 EXACTLY  (q(-1) = 3315 != 0).

horn 1  v_t(R) >= a :  every K-term has order >= 3a  =>  30 >= 3a  =>  a <= 10.
                       FALSE for a >= 11.
horn 2  a + 2*rho = 30, rho = v_t(R) < a  =>  rho = (30-a)/2 in Z  =>  a EVEN.
```

| `a` | horn 1 feasible | horn 2 `rho` candidates | verdict |
|---:|---|---|---|
| 11 | no | NONE | **DEAD** (parity) |
| 12 | no | 9 | alive, `v_t(R) = 9` |
| 13 | no | NONE | **DEAD** (parity) |
| 14 | no | 8 | alive, `v_t(R) = 8` |
| 15 | no | NONE | **DEAD** (parity) |

Control (check B4): horn 1 stays feasible at `a = 8, 9, 10`, so the test is not a constant refutation and the standard regime is untouched.

> This kill is **branch-independent**. It removes `a_t in {11, 13, 15}` on **both** T1 and T2 -- 34 of the 52 branches -- with no cap and no residue argument.

---

## 4. The T2 half of the alternate regime is EMPTY -- two independent routes

### 4.1 Cap-free route (the one to quote)

```
e | Phi        =>  e = gamma*t^a*Pi,  deg Pi = sum b,  ord_t(Pi) = 0
R | e^2  (T2)  =>  e^2 = -6*R*W,  W = d0 + s^2 + d2*s  polynomial
R = c*t^rho    =>  W = -(gamma^2/(6c)) * t^(2a-rho) * Pi^2
e*R | Phi (T2) =>  Psi := 2*Phi/(e*R) = A*t^n*q_rem,  n = 30-a-rho,  A != 0
K              =>  Psi = 3*R - 6*W*X,  X := d2 + 3*s  polynomial

        6*W*X  =  3*c*t^rho - A*t^n*q_rem .

The trichotomy gives rho = (30-a)/2, hence n = rho EXACTLY, so the right
side is t^rho*(3*c - A*q_rem).  X polynomial therefore needs

        2*a - rho  <=  rho + ord_t(3*c - A*q_rem)  <=  rho + (4 - sum b),

i.e.    w := 3*a - 30  <=  4 - sum b .
```

| branch | `a` | `sum b` | `rho` | `w = 3a-30` | `4 - sum b` | verdict |
|---|---:|---:|---:|---:|---:|---|
| `a12_b0000_T2` | 12 | 0 | 9 | 6 | 4 | **DEAD** |
| `a12_b1000_T2` | 12 | 1 | 9 | 6 | 3 | **DEAD** |
| `a12_b1100_T2` | 12 | 2 | 9 | 6 | 2 | **DEAD** |
| `a12_b1110_T2` | 12 | 3 | 9 | 6 | 1 | **DEAD** |
| `a14_b0000_T2` | 14 | 0 | 8 | 12 | 4 | **DEAD** |
| `a14_b1000_T2` | 14 | 1 | 8 | 12 | 3 | **DEAD** |

The single escape is `sum b = 4`, where `q_rem` is a nonzero constant and `3c - A*q_rem` may vanish identically (then `X = 0`). It **does not occur in this universe** -- but for a cap reason, not a lemma reason: `sum b = 4` with `a` even and `a >= 12` needs `deg e = a + 4 >= 16 > 15`, so no such stratum exists. Flagged, not swept: it is the ONE gap in the otherwise cap-free T2 emptiness.

**Field scope.** `Pi` and `q_rem` live over the `q`-splitting field, but the only facts the argument uses are `deg` and `ord_{y=-1}` -- multiplicity data, invariant under any base change. **No square class over `Q` and no rationality of a root is claimed**, so this route is immune to the `FIELD_SCOPE_AUDIT.md` objection in the same way `T1_BRANCH.md` T6 is. (Section 4.2's `|B| = 1, 2` tests are gcd/Groebner computations over `Q` that exploit `q` being irreducible over `Q`; that is the cross-check, not the primary route.)

### 4.2 Cap-dependent route (the committed engine, as a cross-check)

`divisor_consequences.t2_cell_verdict('sub1', a, b)` sweeps `rho` over `0..18` and applies N1-N5. Every alternate-regime T2 branch comes back `DEAD`, **including the odd `a`** that section 3 already removed:

| branch | engine verdict | first surviving `rho` | why |
|---|---|---|---|
| `a11_b0000_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |
| `a11_b1000_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |
| `a11_b1100_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |
| `a11_b1110_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |
| `a11_b1111_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |
| `a11_b2000_T2` | DEAD | none | b_i >= 2 at a simple q-root (e \| Phi) |
| `a11_b2100_T2` | DEAD | none | b_i >= 2 at a simple q-root (e \| Phi) |
| `a11_b2110_T2` | DEAD | none | b_i >= 2 at a simple q-root (e \| Phi) |
| `a11_b2200_T2` | DEAD | none | b_i >= 2 at a simple q-root (e \| Phi) |
| `a11_b3000_T2` | DEAD | none | b_i >= 2 at a simple q-root (e \| Phi) |
| `a11_b3100_T2` | DEAD | none | b_i >= 2 at a simple q-root (e \| Phi) |
| `a11_b4000_T2` | DEAD | none | b_i >= 2 at a simple q-root (e \| Phi) |
| `a12_b0000_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |
| `a12_b1000_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |
| `a12_b1100_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |
| `a12_b1110_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |
| `a12_b2000_T2` | DEAD | none | b_i >= 2 at a simple q-root (e \| Phi) |
| `a12_b2100_T2` | DEAD | none | b_i >= 2 at a simple q-root (e \| Phi) |
| `a12_b3000_T2` | DEAD | none | b_i >= 2 at a simple q-root (e \| Phi) |
| `a13_b0000_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |
| `a13_b1000_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |
| `a13_b1100_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |
| `a13_b2000_T2` | DEAD | none | b_i >= 2 at a simple q-root (e \| Phi) |
| `a14_b0000_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |
| `a14_b1000_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |
| `a15_b0000_T2` | DEAD | none | no rho in 0..18 survives N1-N5 |

Controls (check S3c): the same engine returns **ALIVE** for `a10_b0000_T2`, `a8_b0000_T2`, `a6_b1111_T2` and **DEAD** for `a9_b1000_T2`, `a7_b1110_T2` in the standard regime -- it is not a constant refutation. This reproduces `DIVISOR_CONSEQUENCES.md` sec.8's `a_t <= 10 on T2` by-product, which that file correctly declined to call an unblocking of the `FRONTIER_REBUILD.md` sec.7 lead; here the *missing* T1 half is supplied by section 3, and the lead is unblocked to the extent stated in section 7.

---

## 5. The filter chain, stage by stage

| stage | scope note | kills | alive after |
|---|---|---:|---:|
| `S1  e\|Phi  (D2: b_i <= 1)` | branch-free, cap-free, regime-free | 22 | 30 |
| `S1b e\|Phi  (D1: deg e = a + sum b)` | cap-dependent ONLY through deg e <= 15 | 0 | 30 |
| `S2  place trichotomy at y = -1` | branch-free, cap-free; the a >= 11 horn | 18 | 12 |
| `S3  T2 divisor normal form (T2 ONLY)` | e*R\|Phi, R\|e^2, R = c*t^rho -- NEVER applied to a T1 branch | 6 | 6 |

### Unordered attribution -- what each lemma kills on its own, out of 52

| lemma applied ALONE | branches killed |
|---|---:|
| S1 e\|Phi (b_i<=1) | 22 |
| S1b e\|Phi (defect 0 vs cap) | 0 |
| S2 trichotomy (a odd) | 34 |
| S3 T2 divisor (engine) | 26 |

Stages compose in the order shown and are not double-counted; the unordered column overlaps by construction.

### Whole-branch kills, itemised

| branch | killed by | reason |
|---|---|---|
| `a11_b2000_T1` | `S1` | b_1 = 2 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a11_b2000_T2` | `S1` | b_1 = 2 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a11_b2100_T1` | `S1` | b_1 = 2 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a11_b2100_T2` | `S1` | b_1 = 2 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a11_b2110_T1` | `S1` | b_1 = 2 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a11_b2110_T2` | `S1` | b_1 = 2 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a11_b2200_T1` | `S1` | b_1 = 2 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a11_b2200_T2` | `S1` | b_1 = 2 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a11_b3000_T1` | `S1` | b_1 = 3 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a11_b3000_T2` | `S1` | b_1 = 3 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a11_b3100_T1` | `S1` | b_1 = 3 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a11_b3100_T2` | `S1` | b_1 = 3 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a11_b4000_T1` | `S1` | b_1 = 4 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a11_b4000_T2` | `S1` | b_1 = 4 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a12_b2000_T1` | `S1` | b_1 = 2 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a12_b2000_T2` | `S1` | b_1 = 2 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a12_b2100_T1` | `S1` | b_1 = 2 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a12_b2100_T2` | `S1` | b_1 = 2 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a12_b3000_T1` | `S1` | b_1 = 3 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a12_b3000_T2` | `S1` | b_1 = 3 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a13_b2000_T1` | `S1` | b_1 = 2 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a13_b2000_T2` | `S1` | b_1 = 2 >= 2 at a simple q-root; ord_{r} Phi = 1 and e \| Phi forbid it |
| `a11_b0000_T1` | `S2` | a = 11: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 19/2 to be an integer < a -- it is not (a odd) |
| `a11_b0000_T2` | `S2` | a = 11: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 19/2 to be an integer < a -- it is not (a odd) |
| `a11_b1000_T1` | `S2` | a = 11: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 19/2 to be an integer < a -- it is not (a odd) |
| `a11_b1000_T2` | `S2` | a = 11: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 19/2 to be an integer < a -- it is not (a odd) |
| `a11_b1100_T1` | `S2` | a = 11: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 19/2 to be an integer < a -- it is not (a odd) |
| `a11_b1100_T2` | `S2` | a = 11: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 19/2 to be an integer < a -- it is not (a odd) |
| `a11_b1110_T1` | `S2` | a = 11: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 19/2 to be an integer < a -- it is not (a odd) |
| `a11_b1110_T2` | `S2` | a = 11: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 19/2 to be an integer < a -- it is not (a odd) |
| `a11_b1111_T1` | `S2` | a = 11: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 19/2 to be an integer < a -- it is not (a odd) |
| `a11_b1111_T2` | `S2` | a = 11: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 19/2 to be an integer < a -- it is not (a odd) |
| `a13_b0000_T1` | `S2` | a = 13: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 17/2 to be an integer < a -- it is not (a odd) |
| `a13_b0000_T2` | `S2` | a = 13: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 17/2 to be an integer < a -- it is not (a odd) |
| `a13_b1000_T1` | `S2` | a = 13: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 17/2 to be an integer < a -- it is not (a odd) |
| `a13_b1000_T2` | `S2` | a = 13: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 17/2 to be an integer < a -- it is not (a odd) |
| `a13_b1100_T1` | `S2` | a = 13: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 17/2 to be an integer < a -- it is not (a odd) |
| `a13_b1100_T2` | `S2` | a = 13: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 17/2 to be an integer < a -- it is not (a odd) |
| `a15_b0000_T1` | `S2` | a = 15: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 15/2 to be an integer < a -- it is not (a odd) |
| `a15_b0000_T2` | `S2` | a = 15: horn 1 (v_t(R) >= a) gives 30 >= 3a, false; horn 2 needs rho = (30-a)/2 = 15/2 to be an integer < a -- it is not (a odd) |
| `a12_b0000_T2` | `S3` | CAP-FREE: t-order of X: needs 2a-rho = 15 <= rho + ord_t(3c - A*q_rem) <= 9 + deg(3c - A*q_rem) = 9 + 4 = 13.  15 > 13, and the numerator cannot vanish identically because deg q_rem = 4 > 0 \|\| ENGINE(DEAD): no rho in 0..18 survives N1-N5 |
| `a12_b1000_T2` | `S3` | CAP-FREE: t-order of X: needs 2a-rho = 15 <= rho + ord_t(3c - A*q_rem) <= 9 + deg(3c - A*q_rem) = 9 + 3 = 12.  15 > 12, and the numerator cannot vanish identically because deg q_rem = 3 > 0 \|\| ENGINE(DEAD): no rho in 0..18 survives N1-N5 |
| `a12_b1100_T2` | `S3` | CAP-FREE: t-order of X: needs 2a-rho = 15 <= rho + ord_t(3c - A*q_rem) <= 9 + deg(3c - A*q_rem) = 9 + 2 = 11.  15 > 11, and the numerator cannot vanish identically because deg q_rem = 2 > 0 \|\| ENGINE(DEAD): no rho in 0..18 survives N1-N5 |
| `a12_b1110_T2` | `S3` | CAP-FREE: t-order of X: needs 2a-rho = 15 <= rho + ord_t(3c - A*q_rem) <= 9 + deg(3c - A*q_rem) = 9 + 1 = 10.  15 > 10, and the numerator cannot vanish identically because deg q_rem = 1 > 0 \|\| ENGINE(DEAD): no rho in 0..18 survives N1-N5 |
| `a14_b0000_T2` | `S3` | CAP-FREE: t-order of X: needs 2a-rho = 20 <= rho + ord_t(3c - A*q_rem) <= 8 + deg(3c - A*q_rem) = 8 + 4 = 12.  20 > 12, and the numerator cannot vanish identically because deg q_rem = 4 > 0 \|\| ENGINE(DEAD): no rho in 0..18 survives N1-N5 |
| `a14_b1000_T2` | `S3` | CAP-FREE: t-order of X: needs 2a-rho = 20 <= rho + ord_t(3c - A*q_rem) <= 8 + deg(3c - A*q_rem) = 8 + 3 = 11.  20 > 11, and the numerator cannot vanish identically because deg q_rem = 3 > 0 \|\| ENGINE(DEAD): no rho in 0..18 survives N1-N5 |

---

## 6. The 6 survivors -- state-level, and what is left to do

| branch | `a` | `sum b` | `deg e` forced | degree states | after inf sweep | + D1 | after combination | + D1 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `a12_b0000_T1` | 12 | 0 | 12 | 4480 | 459 | 89 | 447 | 86 |
| `a12_b1000_T1` | 12 | 1 | 13 | 3360 | 370 | 74 | 325 | 68 |
| `a12_b1100_T1` | 12 | 2 | 14 | 2240 | 296 | 58 | 235 | 51 |
| `a12_b1110_T1` | 12 | 3 | 15 | 1120 | 238 | 238 | 164 | 164 |
| `a14_b0000_T1` | 14 | 0 | 14 | 2240 | 227 | 52 | 213 | 49 |
| `a14_b1000_T1` | 14 | 1 | 15 | 1120 | 175 | 175 | 144 | 144 |
| **total** | | | | 14560 | 1765 | 686 | 1528 | **562** |

`+ D1` is the `e | Phi` defect-0 consequence `deg e = a + sum(b_i)` **exactly**, applied to the recorded degree states. It is a *state* reduction, not a kill: every one of the 6 branches keeps states.

On each survivor the exact residual obligations are unchanged in kind: `v_t(R) = 9/8` is now pinned, `R` has `v_beta(R) = 0` at every marked root (trichotomy at `beta`, since `v_beta(Phi) = v_beta(e) = 1`), and the descending congruences `(D_t)`/`(D_p)` through `h_5` plus the bottom close `E^21 h_0 + u r_0 = 0` remain open.

---

## 7. PROVED / CONSTRAINT-ONLY / REDUCTION / FIELD-DEPENDENT

### 7.1 Compiler-proved WHOLE-BRANCH kills (46 branches)

| kill | branches | evidence |
|---|---:|---|
| `S1  e\|Phi  (D2: b_i <= 1)` | 22 | PROVED -- exact identity / finite valuation enumeration, char 0 |
| `S2  place trichotomy at y = -1` | 18 | PROVED -- exact identity / finite valuation enumeration, char 0 |
| `S3  T2 divisor normal form (T2 ONLY)` | 6 | PROVED -- exact identity / finite valuation enumeration, char 0 |

**Every kill listed is cap-free.** The one cap-dependent stage, `S1b` (`deg e = a + sum b <= 15`), kills **0** branches and is in section 5 only for completeness; the section 4.2 engine cross-check is cap-dependent but the section 4.1 route it cross-checks is not.

The caps do, however, still set the **scope**: which `(a, b)` strata exist at all is `a + sum(b) <= 15` (section 1). Two consequences, stated explicitly rather than buried: `a >= 16` is untested here, and `sum b = 4` -- the one configuration that escapes the section 4.1 t-order test -- never arises for even `a >= 12` only because `deg e = a + 4 >= 16` would exceed the cap. **Widening `deg e` would reopen exactly those two families and nothing else.**

### 7.2 LOCAL CONSTRAINTS ONLY (no branch dies)

* `e | S` and `T = -R*(S/e + d2) - d1*e/2` collapse the spare ansatz (45 -> 18, branch-independent, `DIVISOR_CONSEQUENCES.md` sec.9). `dm4` is **not** a spare. No alternate-regime branch dies from this.
* The T1 replacements `R | e^2*(e + 3*d1*s)` and `R | e^2*(s^2 - d0)`. Real constraints; **not evaluated as kills here**.
* `W := e*S - R^2` with `W^2 = R^4 + d2*e^2*R^2 + d1*e^3*R + d0*e^4` and the even-multiplicity condition (`T1_BRANCH.md` T6). At `y=-1` the quartic has unique minimum `4*rho` (even) on every survivor, so it is consistent and kills nothing here.

### 7.3 STATE REDUCTIONS

* **D1** (`deg e = a + sum b` exactly): 1528 -> 562 states on the survivors (combination layer), i.e. 966 states removed inside surviving branches.
* **The trichotomy** pins `v_t(R)` to a single value per `a`; `R` is not a recorded state coordinate in this lane, so this cannot be cashed at state level yet.

### 7.4 FIELD-DEPENDENT observations (C08/C20 -- CONSTRAINT, not KILL)

`FIELD_SCOPE_AUDIT.md` downgrades C08/C20 from KILL to CONSTRAINT. Census under **both** settings:

| setting | branches killed by C08/C20 | states killed | non-obligatory ties |
|---|---:|---:|---:|
| C08/C20 ON | 0 | 0 | 249 |
| C08/C20 OFF | 0 | 0 | 249 |

**CONFIRMED, and recomputed rather than quoted.** Check `E1` walks all 3102 per-state rows of `alt_residue_congruences.json` and finds every one classified `CONSTRAINT`, with **0** states whose REQUIRED level-0 tie is a forbidden C08/C20 support -- which is the only way the downgrade could move a verdict. That independently reproduces the file's own `kill_audit`: `kills_on == kills_off`, `killed_at_depth1 = 0`, C08/C20 as a REQUIRED obligation **0** times (they occur 523 times as non-obligatory tropical ties). None of `S1`, `S1b`, `S2`, `S3` uses C08 or C20 in any form, so the survivor set above is **identical** under both settings. This reproduces `FIELD_SCOPE_AUDIT.md` sec.4.6 ("the alternate regime: zero").

### 7.5 Premises this census still rests on

* the sub1 stripped caps `deg d2 <= 6, deg d1 <= 9, deg d0 <= 12, deg e <= 15, deg R <= 18, deg S <= 21` (`CAPS_AUDIT.md` sec.3 [P1]-[P3]). They fix the SCOPE (which strata exist: `a + sum b <= deg e cap`) and are used by `S1b` and by the section 4.2 cross-check. **No kill in section 7.1 uses them.**
* `Phi = -(1/6630)*(y+1)^30*q`, `deg Phi = 34`, `q` squarefree and irreducible over `Q`, `q(-1) = 3315` (recomputed in B2)
* the canonical `G1,G2,G3,G5` normalisation with `coeff(G5, Phi) = 1`

---

## 8. The external synthesis, checked

An external review inferred a six-branch alternate frontier, all T1, and explicitly said it should not be quoted because it was not compiler-emitted. Recomputed here from the 52-branch universe:

```
external : a12_b0000_T1, a12_b1000_T1, a12_b1100_T1, a12_b1110_T1, a14_b0000_T1, a14_b1000_T1
compiler : a12_b0000_T1, a12_b1000_T1, a12_b1100_T1, a12_b1110_T1, a14_b0000_T1, a14_b1000_T1
```

> **AGREES exactly.** The six-branch frontier is now compiler-emitted and may be quoted, with the grading of section 7.

What is **proved dead** and what is **proved alive** are not the same grade, and the distinction must survive the quote:

* **Proved DEAD (46 branches):** every kill in section 5 is an exact identity or a finite valuation enumeration in characteristic 0. Nothing is inferred.
* **Proved ALIVE (0 branches):** *none* of the six is proved to contain a solution. They are branches no current lemma refutes. "Survivor" means OPEN, and every one of them still carries the descending congruences and the bottom close as unmet obligations.
* **INFERRED (0):** no step in this chain is inferred. The one place where an inference could have entered -- carrying `R = c*(y+1)^rho` onto a T1 column -- is blocked structurally (G6).

---

## 8b. What the superseded 27-branch residue loses

The chain above never reads `ALT_REGIME.md` or `ALT_REGIME_L2.md`. This section only records that the two accounts are **compatible**: every survivor lies inside the old 27, so the new lemmas strictly add kills and contradict nothing (check **F2**).

The 21 branches the 27-list loses:

`a11_b0000_T1`, `a11_b0000_T2`, `a11_b1000_T1`, `a11_b1000_T2`, `a11_b1100_T1`, `a11_b1100_T2`, `a11_b1110_T1`, `a11_b1110_T2`, `a11_b1111_T1`, `a11_b1111_T2`, `a11_b3000_T1`, `a11_b3000_T2`, `a11_b3100_T2`, `a12_b0000_T2`, `a12_b1000_T2`, `a12_b1100_T2`, `a12_b1110_T2`, `a12_b3000_T1`, `a13_b0000_T2`, `a13_b1000_T2`, `a14_b0000_T2`

The other 25 alternate branches were already killed by `ALT_REGIME.md` / `ALT_REGIME_L2.md`. **Every one of them is re-killed here independently**, with this stage breakdown -- which is why the six-branch frontier does not inherit those two rounds as premises:

| stage that re-kills it | branches |
|---|---:|
| `S1  e\|Phi  (D2: b_i <= 1)` | 18 |
| `S2  place trichotomy at y = -1` | 6 |
| `S3  T2 divisor normal form (T2 ONLY)` | 1 |

---

## 9. Provenance -- every input, its hash, and the command that reads it

| artifact | sha256(16) | role |
|---|---|---|
| `split_place_ledger_sub1.json` | `cd331af7630f4e4e` | the 26-strata / 52-branch alternate universe |
| `alt_inf_sweep.json` | `4019b5de5e14b480` | degree (max-plus) layer, 38360 -> 4690 |
| `alt_combined.json` | `12dd5f9c75b9d5ad` | finite-place intersection, 4690 -> 3102 |
| `alt_residue_congruences.json` | `290522c4eaeeb770` | residue congruences + the C08/C20 counterfactual |

| imported checker | what it certifies | its own command |
|---|---|---|
| `divisor_syzygy.py` | K-syzygy residual 0, `e \| Phi` | `python -u divisor_syzygy.py --quiet` |
| `divisor_consequences.py` | `e \| S`, T2 normal form, `t2_cell_verdict` | `python -u divisor_consequences.py --quiet` |
| `t1_branch.py` | the place trichotomy, `place_trichotomy` | `python -u t1_branch.py --quiet` |
| `cascade_engine.py`, `full_system_bridge.py` | the sub1 cap tables | (read-only import) |

Checker: `python -u alt_rebuild.py --quiet` (exit 0/1). It reloads every input, re-verifies L1-L4 symbolically, re-runs the whole chain and requires every number in this file. A moved input is an `AssertionError`, not a smaller census.

### Checks

| tag | result |
|---|---|
| `A1_universe_52` | PASS |
| `A2_prior_27` | PASS |
| `A3_caps` | PASS |
| `B1_ksyzygy_residual_0` | PASS |
| `B2_phi_arithmetic` | PASS |
| `B3_e_divides_S` | PASS |
| `B4_trichotomy` | PASS |
| `B5_D3_vacuous_sub1` | PASS |
| `G6_T2_only_witness` | PASS |
| `G7_spine_out_of_scope` | PASS |
| `G8_pole_gate_fails` | PASS |
| `S3c_engine_controls` | PASS |
| `S3_T2_regime_empty` | PASS |
| `D_state_census` | PASS |
| `E1_c08_c20_invariant` | PASS |
| `F2_consistent_with_27` | PASS |
| `F3_pre27_all_rekilled` | PASS |
| `F1_external_compare` | PASS |

`18/18` pass.
