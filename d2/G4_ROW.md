# G4_ROW -- the lambda row (`G4`, u-weight 192): derivation, imposition, and the measured census delta

> Machine-generated by `python -u g4_row.py`. `python -u g4_row.py --quiet` re-derives everything and exits nonzero on any drift. Read-only on every other artifact; this lane owns `g4_row*` only.


## 0. Verdict up front

> **`G4` re-derives cleanly from primitives, and `dm12` cancels identically** -- the repo's stated reason for dropping the `j = 4` Q-slice does not survive the arithmetic, exactly as `TRANSFORM_AUDIT.md` F1 reports. The row is real, it is weighted-homogeneous at u-weight 192 (the missing rung), and it is not implied by the four used rows.

> **Imposing it kills NOTHING** -- 0 cells, 0 flag cases, 0 states -- in standard `sub1` (both C08/C20 settings) and in all six alternate-regime T1 branches. That is not a weak measurement: section 6 **proves** that no cell/state-level counting test built on the lambda row alone can ever kill, because the row vanishes identically at `dm2 = dm3 = dm4 = 0` with `lambda = 0`, for every `(d2, d1, d0, dm1)`. The +32/+48 equations are real, and they are **elimination-only** content.

> Standard `sub2` is EMPTY and **cannot** be resurrected: the augmented system's generator set is a superset and `lambda` is existentially quantified (section 5.3, a theorem, no computation).


## 1. Re-derivation from primitives

Nothing below is imported from `TRANSFORM_AUDIT.md`; the comparison against its transcription is check `A5`, run *after* the derivation.

```
S      = 1 + d2*u^2 + d1*u^3 + d0*u^4 + sum_{k>=1} dm_k*u^{4+k}
D2(k) := [u^{8+k}] S^2      (the P-side: P_-k = 0 for every k >= 1)
D3(j) := [u^{12+j}] S^3     (the Q-side)
```

The pipeline uses `D2(k)` for `k in {1..7, 9}` and `D3(j)` for `j in {1,2,3,5}`; it drops the pair `D2(8)`, `D3(4)`.

| step | fact | check |
|---|---|---|
| 1 | `dm12` occurs in **no** used slice, and in **both** dropped ones | `A1`, `A1b` |
| 2 | both dropped slices are degree 1 in `dm12`, coefficients **2** and **3** | `A2`, `A2b` |
| 3 | therefore `D3(4) - (3/2)*D2(8)` is `dm12`-free -- **identically** | `A3` |
| 4 | and no spare above `dm10` survives | `A3b` |
| 5 | the P-side chain `k = 1..7` (linear, coefficient 2) eliminates `dm5..dm11` | `A4` |
| 6 | what is left involves **only** the seven G-system window variables | `A5a` |

The dropped pair is therefore equivalent to `{ a definition of dm12 , G4 }` (`A6`, `A6b`): `D2(8)` after the chain is `2*dm12 + (dm12-free)`, which merely *names* a fresh unknown and carries no content, and the residue is

```
G4 = -3*(2*d0*dm1*dm3 + d0*dm2**2 + 2*d1*dm2*dm3 + d2*dm3**2 + dm1**2*dm2 - dm4**2)/2
```

```
   = -(3/2)*( 2*d0*dm1*dm3 + d0*dm2^2 + 2*d1*dm2*dm3
              + d2*dm3^2 + dm1^2*dm2 - dm4^2 )
```

**Six** monomials, not five: `TRANSFORM_AUDIT.md` sec.2.1's prose "five-monomial quintic" is a slip (its sec.7 PROVED item 2 lists all six, correctly). Check `A5b`. Nothing downstream depends on the count.


## 2. Grading -- G4 is the missing rung

| variable | `d2` | `d1` | `d0` | `dm1` | `dm2` | `dm3` | `dm4` | `Phi` |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| u-weight `12k` | 24 | 36 | 48 | 60 | 72 | 84 | 96 | 204 |

| monomial | weight | sub2 stripped deg | sub1 stripped deg |
|---|---:|---:|---:|
| `3*dm4**2/2` | 192 | 32 | 48 |
| `-3*d0*dm2**2/2` | 192 | 32 | 48 |
| `-3*d2*dm3**2/2` | 192 | 32 | 48 |
| `-3*dm1**2*dm2/2` | 192 | 32 | 48 |
| `-3*d0*dm1*dm3` | 192 | 32 | 48 |
| `-3*d1*dm2*dm3` | 192 | 32 | 48 |

Every monomial is at weight **192** and attains the stripped degree cap (`192/6 = 32` in sub2, `192/4 = 48` in sub1) exactly -- checks `B1`, `B4-sub2`, `B4-sub1`. `full_system_bridge.check_homogeneity()` asserts the consumed weights are `{156, 168, 180, 204}` (`B3`); in bridge units the ladder is **156, 168, 180, [192], 204** and `G4` lands on the gap.


## 3. How `lambda` enters -- the exact form of the condition

`lambda` is the alpha-strip coefficient `alpha_-1`, a **scalar of `K`** (`T6_SELECTION_AUDIT.md` sec.4 -- the same premise the normal form `Q = C^3 + lambda*C^-1 + F` already rests on; this lane does not re-prove it, see the INFERRED list). With `C4 = y^7*(y+1)` after the A2 normalisation, `C4^28 = y^196*(y+1)^28` (`C1`), and `G4_full = y^192*G4_stripped` by weight-homogeneity, so

```
 G4_full + lambda*C4^28 = 0   <=>   G4_stripped = -lambda*y^4*(y+1)^28
```

> **This is far stronger than "there exists some lambda".** It says the stripped row is a **scalar multiple of one fixed degree-32 polynomial**: every other coefficient must vanish. `y^4*(y+1)^28` is monic of degree 32 with `ord_y = 4` (`C3`), so the top coefficient equation reads `lambda = -[y^32] G4_stripped` -- **`lambda` is determined, not free** (`C3b`).


| row | u-weight | sub2 equations | sub1 equations |
|---|---:|---:|---:|
| G1 | 156 | 27 | 40 |
| G2 | 168 | 29 | 43 |
| G3 | 180 | 31 | 46 |
| G5 | 204 | 35 | 52 |
| **used total** | | **122** | **181** |
| **G4 (lambda row)** | **192** | **33 - 1 = 32** | **49 - 1 = 48** |
| | | **+26.2 %** | **+26.5 %** |

Checks `C4`, `C5`, `C5b`. Reproduces `TRANSFORM_AUDIT.md` sec.2.4 exactly.


## 4. Not implied by the used rows (re-checked, not inherited)

The canonical guard is asserted first: **`G5 = G5body + Phi` with `coeff(G5, Phi) == 1`** and `G5body` free of `Phi` (`D0`). A stale `2*Phi` transcription was a real bug here.

* **`D1` (fresh).** A witness point searched for *in this run*, on `V(G1,G2,G3,G5)`, with `G4 != 0`: `{'d2': '0', 'dm1': '1', 'dm2': '1', 'd1': '-25/3', 'd0': '25/3', 'dm3': '2', 'dm4': '13/6', 'Phi': '9/2'}`, there `G4 = -167/24`.

* **`D2`/`D2b`.** `TRANSFORM_AUDIT.md`'s witness `(2*Phi-3, -1/3, 1, 1, 0, 1, 1/6)` re-checked: all four used rows vanish identically in `Phi`, and `G4 = -3*Phi + 37/24 != 0`. **Reproduces.**

* **`D3`/`D3b`.** The 1-parameter family lies on all four used rows for every `w`, and on it `G4 = (-72*Phi*w**4 + 36*w**5 + 1)/(24*w**2)` -- non-constant, so the used rows pin neither `G4` nor its divisor. **Reproduces.**


## 5. Imposing it


### 5.1 In the bridge's coordinates (the drop-in)

`g4_row.g4_row_equations(G4, subs_map, regime)` returns the `cap+1` y-coefficients of

```
 G4_stripped(subs_map) + lam * y^4*(y+1)^28  ==  0
```

with `lam` one new scalar unknown -- exactly what `full_system_bridge.augment()` would append as a fifth generator. Measured on live ansaetze:


| regime | row degree | coefficient equations | rows containing `lam` | spare unknowns |
|---|---:|---:|---:|---:|
| sub2 | 32 | 33 | 29 | 45 |
| sub1 | 48 | 49 | 29 | 66 |

`lam` appears in exactly **29** rows in both regimes -- the nonzero coefficients of `y^4*(y+1)^28`, i.e. `y^4 .. y^32` -- and the `y^32` row pins it. Checks `F1-*`, `F2-*`.


**Freedom balance, measured on those same live ansaetze** (not the theoretical maxima):


| regime | used-row equations | unknowns | + lambda row | unknowns | net gain |
|---|---:|---:|---:|---:|---:|
| sub2 | 122 | 54 | 155 | 55 | **+32** |
| sub1 | 176 | 75 | 225 | 76 | **+48** |

> The `sub2` used-row count is **122**, exactly the theoretical maximum of section 3. The `sub1` count is **176**, not the theoretical 181, because the probe ansatz has `deg e = 10` rather than the `sub1` cap 15, so `G1, G2, G3, G5` fall short of their degree ceilings. The lambda row still reaches its full 49 -- its `dm4^2` monomial attains degree 48 from the spare cap alone, independent of `deg e`. The `+48` is therefore **not** an artefact of a favourable ansatz.


Checks `F2b-sub2`, `F2b-sub1`.


### 5.2 In the collapsed coordinates the live lanes actually use

`DIVISOR_CONSEQUENCES.md` sec.9 / `ALT_FRONTIER_V2.md` sec.7.2 collapse the spare ansatz 45 -> 18 via `e | S` (write `S = e*s`) and `dm4 = -R*(s + d2) - d1*e/2` (so `dm4` is not a spare). Written there, `G1 == 0` identically -- it *is* the collapse (`E1`) -- and the lambda row becomes

```
G4|collapsed = -(3/2)*( R^2*A + e^2*B + e*R*d1*(s - d2) )
      A = d0 - (s + d2)^2
      B = R + 2*d0*s + d2*s^2 - d1^2/4
```

(check `E2`; `e = dm1`, `R = dm2`, `s = dm3/dm1`). Two further facts that make it usable:

* **`E4`.** `G2|collapsed` **is** the `T1_BRANCH.md` T6 relation `W^2 = R^4 + d2*e^2*R^2 + d1*e^3*R + d0*e^4` with `W = e*S - R^2`, up to the factor `3/2`. So T6 is not independent information -- it is `G2`.

* **`E3`/`E3b`.** `G2` is linear in `d0`, so eliminating `d0` gives a **sigma-free** lambda row: with `p := d2 + 2*s`,

```
(16/9)*Krow =  e^4*(4*s^2*p - d1^2) - 4*d1*e^3*R*(d2 + s)
             - 4*p^2*e^2*R^2 - 4*d1*e*R^3 - 4*p*R^4 + 4*e^4*R
Krow = -(3/2)*e^2 * G4|collapsed   (mod G2)   =   (3/2)*lam*e^2*y^4*(y+1)^28
```

`Krow` removes the largest unknown block (13 coefficients of `d0` in the alternate regime) at the price of the factor `e^2`. Section 6's order calculus and section 8's probe use `G4|collapsed` directly; `Krow` is the sharper handle for the cancellation ladder recorded OPEN in section 9 item 23.


### 5.3 The `sub2` regression is a theorem, not a measurement

> Adding the lambda row replaces the generator set `{G1,G2,G3,G5}` by the **superset** `{G1,G2,G3,G5} u {G4_str + lam*y^4*(y+1)^28}` and adds one existentially quantified scalar `lam`. Any point of the augmented variety satisfies every old generator by construction, so the projection to the old variables lands **inside** the old variety. An EMPTY cell therefore stays empty. Check `F3`. **No resurrection is possible in standard `sub2`, and none was computed or needed.**


## 6. Why the counting-level answer is zero, and why that is a PROOF

> **Every monomial of `G4` contains `dm2`, `dm3` or `dm4`** (`G1`). Hence `G4 = 0` at `dm2 = dm3 = dm4 = 0`, and `lambda = 0` then satisfies the divisor condition **for every** `(d2, d1, d0, dm1)` -- on every cell, every flag case, every state, in every window and both regimes.

So no filter of the `divisor_filter.py` species (an order or degree count against the state's data) can ever kill using the raw lambda row: the spares are capped **above** and free **below**, so the row can always be driven to zero (`G5`). The row's content is **elimination-only** -- it bites jointly with `G1,G2,G3,G5`, never alone.


On the *collapsed* ansatz the trivial point is unavailable on T1 (`R = s = 0` gives `G4 = (3/8)*d1^2*e^2 != 0` when `d1 != 0`, check `G2`), so a counting test is at least conceivable there. It still yields nothing:


**Order calculus at `y = -1`.** With `a := v_t(e)`, `rho := v_t(R)`, `v_t(s) >= 0`, the three groups of `G4|collapsed` have `t`-orders `>= 2*rho`, `>= 2*a`, `>= a + rho`, so `v_t(G4|collapsed) >= 2*min(a, rho)`; the divisor condition forces it to be **exactly 28 when `lambda != 0`** (and infinite when `lambda = 0`, since then `G4_str == 0`). The `T1_BRANCH.md` sec.1.2 trichotomy fixes which horn is available:


| horn | scope | `rho` | `v_t` floor | `lam != 0` demands | consequence |
|---|---|---|---|---|---|
| 1 (`rho >= a`) | `a <= 10` (standard) | `>= a` | `2*a` | floor `<= 28` | `a <= 14` -- **VACUOUS** |
| 2 (`a + 2*rho = 30`) | `a >= 11` even (alternate) | `(30-a)/2` | `30 - a` | floor `<= 28` | `a >= 2` -- **VACUOUS** |

> **And a violation would not be a kill anyway** (`G3c`). `lambda` is a constant that is ALLOWED TO BE ZERO, so `v_t(G4_str) > 28` forces `lambda = 0`, i.e. `G4_str == 0` -- a **case** of the divisor condition, not a contradiction. The order calculus can therefore only ever eliminate the `lambda != 0` branch, never a cell. On the surviving frontier it does not even do that: `floor > 28` needs `a > 14` (horn 1) or `a < 2` (horn 2), and neither occurs.


Checks `G3`, `G3b`. The marked-root test (`v_beta(R) = 0` when `lambda != 0`) is a **re-derivation of an already committed lemma** (`ALT_FRONTIER_V2.md` sec.6, trichotomy at `beta`), not new content (`G4`).


> **Scope of the two claims, kept apart.** The statement about the **raw** row (`G1`, `G1b`) is a PROOF: the row alone constrains nothing at cell/state level, ever. The statement about the **collapsed** row (`G2`, `G3`, `G3b`, `G4`, `G5`) is an EXHAUSTION OF THE NATURAL TESTS -- order at `y = -1`, order at a marked root, order at `y = 0`, and degree -- each computed and each vacuous. It is not a proof that no counting test whatsoever exists on the collapsed row. What it does establish is that the species of test `divisor_filter.py` and `ALT_FRONTIER_V2.md` sec.3-4 use is exhausted, and the row's content lies past it.


## 7. The measured census delta

Baselines are `frontier_rebuild.STAGES` applied to the committed `e | Phi`-filtered universes -- recomputed here, not quoted (`H1`, `H1b`, `H2`).


| window | C08/C20 | cells | flag cases | states | cells killed | flag cases killed | states killed | cells after | flag cases after | states after |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| sub1 | ON | 34 | 314 | 7275 | **0** | **0** | **0** | 34 | 314 | 7275 |
| sub1 | OFF | 34 | 322 | 8889 | **0** | **0** | **0** | 34 | 322 | 8889 |
| sub2 | ON | 0 | 0 | 0 | **0** | **0** | **0** | 0 | 0 | 0 |
| sub2 | OFF | 0 | 0 | 0 | **0** | **0** | **0** | 0 | 0 | 0 |

**Alternate regime** (the six surviving T1 branches, `ALT_FRONTIER_V2.md` sec.6, parsed not typed -- `H4`):


| branch | `a` | horn-2 `rho` | `v_t` floor | `lam != 0` branch open? | verdict |
|---|---:|---:|---:|---|---|
| `a12_b0000_T1` | 12 | 9 | 18 | yes | alive |
| `a12_b1000_T1` | 12 | 9 | 18 | yes | alive |
| `a12_b1100_T1` | 12 | 9 | 18 | yes | alive |
| `a12_b1110_T1` | 12 | 9 | 18 | yes | alive |
| `a14_b0000_T1` | 14 | 8 | 16 | yes | alive |
| `a14_b1000_T1` | 14 | 8 | 16 | yes | alive |

| | branches | states |
|---|---:|---:|
| before | 6 | 562 |
| killed by the lambda row | **0** | **0** |
| after | 6 | 562 |

> **No `g4_row_stage.json` is emitted**, because nothing dies. The file is written only if the census is nonzero; its absence is the result.


## 8. Bounded solver probe -- ATTEMPTED, NOT COMPLETED

`python -u g4_row.py --probe` builds the target below and runs two **sequential** bounded Singular jobs, a CONTROL (`G2, G3, G5` collapsed) and a TEST (the same **plus the lambda row**). It is implemented and wired; it did **not** complete inside its bound in this session and was stopped. **No verdict is recorded, and none is implied** -- an unfinished Groebner run is not evidence of survival, exactly as `137 / 124 / 139` are not verdicts.


Target **`a14_b0000_T1`** (alternate regime, the tightest of the six): `deg e = a + sum(b) = 14` exactly (D1), so `e = gam*(y+1)^14` with ONE unknown; horn 2 pins `v_t(R) = (30-14)/2 = 8` exactly, so `R = (y+1)^8 * sum_{i<=10} rh_i*(y+1)^i` with `rh0 = Rh(-1) != 0`; `deg s <= 7`, `deg d2 <= 6`, `deg d1 <= 9`, `deg d0 <= 12`; saturated by `gam*rh0`. Roughly 51 unknowns (52 with `lam`).


> **Sound over-approximation, stated in advance:** the probe does NOT impose `deg dm4 <= 24` or `d1 != 0` (T1). Dropping necessary conditions can only make a kill harder, so a UNIT would have been genuine; a NON-UNIT would not have been a survival proof either way.


What the attempt did establish, cheaply: the y-coefficient extraction for that ansatz has to be done by dense coefficient-list convolution, not `sympy.expand` on the assembled expression -- the latter is the bottleneck, not the solver. That is implemented in `g4_row.probe`, and a completed run caches to `g4_row_probe.json` so the report can be re-rendered without re-running Singular.


## 9. PROVED / CHECKED / INFERRED / OPEN


**PROVED** (exact symbolic identity or explicit witness, in `g4_row.py`)

1. `dm12` occurs in no used slice and in both dropped ones; both are degree 1 in it with coefficients 2 and 3; `D3(4) - (3/2)*D2(8)` is `dm12`-free identically. (`A1`-`A3b`)

2. The dropped pair is equivalent to `{2*dm12 + ... = 0, G4 = 0}` -- one definition and one condition. (`A6`, `A6b`)

3. `G4 = -(3/2)*(2*d0*dm1*dm3 + d0*dm2^2 + 2*d1*dm2*dm3 + d2*dm3^2 + dm1^2*dm2 - dm4^2)`, six monomials, in exactly the seven G-system window variables. (`A5a`, `A5b`)

4. Every monomial has u-weight 192 and attains the stripped degree cap 32 (sub2) / 48 (sub1). (`B1`, `B4-*`)

5. `C4^28 = y^196*(y+1)^28`, so the condition is exactly `G4_stripped = -lambda*y^4*(y+1)^28`; the target is monic of degree 32 with `ord_y = 4`, so `lambda = -[y^32] G4_stripped` is **determined**. (`C1`-`C3b`)

6. Equation counts 33/49, net +32/+48 against used totals 122/181. (`C4`, `C5`, `C5b`)

7. `G4` is not in `<G1,G2,G3,G5>`: a FRESH witness found in this run, plus the audit's witness and 1-parameter family re-checked. (`D1`-`D3b`)

8. `G5 = G5body + Phi`, `coeff(G5, Phi) == 1`, `G5body` free of `Phi`. (`D0`)

9. On the collapsed ansatz `G1 == 0`, `G4|collapsed = -(3/2)*(R^2*A + e^2*B + e*R*d1*(s-d2))`, and eliminating `d0` against `G2` gives the sigma-free `Krow`. (`E1`-`E3c`)

10. `G2|collapsed` **is** the `T1_BRANCH.md` T6 relation (up to `3/2`). (`E4`, `E4b`)

11. Every monomial of `G4` carries a spare, so `lambda = 0` with `dm2 = dm3 = dm4 = 0` satisfies the divisor condition unconditionally: **no cell/state-level test on the RAW row can kill.** (`G1`, `G1b`, `G5`)

12. MONOTONICITY: the augmented variety projects into the old one, so no empty cell can be resurrected. (`F3`)


**CHECKED** (verified against the repo's own artifacts, not re-proved)

12b. On the COLLAPSED row the four natural counting tests -- order at `y = -1` (both horns), order at a marked root, order at `y = 0`, and degree -- are each computed and each VACUOUS. This exhausts the species of test the pipeline uses; it is **not** a proof that no counting test exists. (`G2`, `G3`, `G3b`, `G4`, `G5`)

13. The freshly derived `G4` equals `TRANSFORM_AUDIT.md` sec.2.1's transcription exactly. (`A5`)

14. `full_system_bridge.WEIGHT` is the same `12k` grading and asserts the consumed ladder `{156,168,180,204}`; 192 is structurally absent. (`B2`, `B3`)

15. `sol4` in `generators.json` equals `-R*(S/e + d2) - d1*e/2`. (`E1b`)

16. Survivor baselines: sub1 ON 34/314/7275, sub1 OFF 34/322/8889, sub2 standard EMPTY -- recomputed from the committed universes. (`H1`-`H2`)

17. The six alternate T1 branches and their 562 states, parsed out of `ALT_FRONTIER_V2.md` sec.6. (`H4`)


**INFERRED** (follows from premises this lane did not re-prove)

18. **`lambda in K` is a constant.** From the alpha-strip / GGV1 leading-form premises (`T6_SELECTION_AUDIT.md` sec.4). **The entire divisor condition stands or falls with this** -- it is the same premise the normal form `Q = C^3 + lambda*C^-1 + F` already rests on, but it is not re-derived here.

19. `C4 = y^7*(y+1)` after the A2 normalisation (`T6_SELECTION_AUDIT.md` sec.4); field-of-definition consequences belong to `FIELD_SCOPE_*`.

20. `e | S` -- used only for the *collapsed* forms of section 5.2 and the order calculus of section 6, never for the imposition of section 5.1. (`ALT_FRONTIER_V2.md` L2, `DIVISOR_CONSEQUENCES.md` sec.9.)

21. The `T1_BRANCH.md` sec.1.2 trichotomy at `y = -1`, used to select the horn in the order calculus. Its scope guards are respected: the `T2`-only relations `R | e^2`, `e*R | Phi`, `R = c*(y+1)^rho` are **not** used anywhere in this file, SPINE's zero-slack count is **not** applied to sub1, and `POLE_THEOREM` Thm 2C is **not** invoked (its gate `3a - 2 < 30` is false for `a >= 11`).


**OPEN**

22. **Whether the lambda row kills by ELIMINATION.** That is where all of its content is (section 6), and it is **not settled here**. The sub1 augmented system carries 66 spare coefficient unknowns before the collapse and 18 after; a full Groebner sweep over 314 flag cases was not attempted, and the single bounded probe of section 8 did not complete inside its bound. Nothing in this file should be read as evidence that the row fails to kill by elimination -- only that it does not kill by counting.

23. The ladder of cancellation conditions the order calculus produces in the alternate regime **on the `lambda != 0` branch**: `v_t(Krow)` must climb from its floor `4*min(a, rho)` to `2*a + 28` -- 16 orders at `a = 12` (36 -> 52), 24 at `a = 14` (32 -> 56). Each step is individually satisfiable, so it is a ladder, not a kill -- but it is cheap, and it is the natural next probe. The `lambda = 0` branch (`Krow == 0` identically) is a separate, and possibly easier, target.

24. Whether `lambda = 0` is forced on any surviving cell. If it is, the condition strengthens from "33/49 coefficients proportional" to "33/49 coefficients zero" and the free scalar is not spent.


## 10. Checks

| id | check | result |
|---|---|---|
| `A0` | t=3 generator control reproduces arXiv:2204.14178 sec.6 | PASS |
| `A1` | dm12 is absent from all 12 USED slices | PASS |
| `A1b` | dm12 IS present in both DROPPED slices (D2(8), D3(4)) | PASS |
| `A2` | both dropped slices are DEGREE 1 in dm12 | PASS |
| `A2b` | coeff(D2(8), dm12) = 2 and coeff(D3(4), dm12) = 3 | PASS |
| `A3` | dm12 CANCELS IDENTICALLY from D3(4) - (3/2)*D2(8) | PASS |
| `A3b` | no spare above dm10 survives the cancellation | PASS |
| `A4` | P-side chain k=1..7 is linear in the fresh spare, coefficient 2 | PASS |
| `A5a` | G4 involves ONLY the seven G-system window variables | PASS |
| `A5` | freshly derived G4 == TRANSFORM_AUDIT.md sec.2.1 transcription | PASS |
| `A5b` | G4 has SIX monomials (TRANSFORM_AUDIT sec.2.1 prose says 'five-monomial'; sec.7 item 2 lists six -- the prose is the slip) | PASS |
| `A6` | D2(8) after the chain is 2*dm12 + (dm12-free), i.e. a DEFINITION | PASS |
| `A6b` | the dropped PAIR is equivalent to {dm12-definition, G4} | PASS |
| `B1` | every monomial of G4 has u-weight exactly 192 | PASS |
| `B2` | full_system_bridge.WEIGHT is the same grading w(d_{4-k}) = 12k | PASS |
| `B3` | the CONSUMED ladder is {156,168,180,204}; 192 is structurally absent | PASS |
| `B3b` | bridge units: 156,168,180,[192],204 -- G4 lands on the gap | PASS |
| `B4-sub2` | G4 stripped degree cap in sub2 is 32, ATTAINED by every monomial | PASS |
| `B4-sub1` | G4 stripped degree cap in sub1 is 48, ATTAINED by every monomial | PASS |
| `C1` | C4^28 = y^196*(y+1)^28 | PASS |
| `C2` | y^192 * (y^4*(y+1)^28) == C4^28  (the strip identity) | PASS |
| `C3` | the divisor y^4*(y+1)^28 has degree 32 and ord_y = 4 | PASS |
| `C3b` | lambda is DETERMINED, not free: [y^32] target == 1, so lambda = -[y^32] G4_stripped | PASS |
| `C4-sub2` | sub2: 33 y-coefficient equations, minus 1 free scalar = +32 | PASS |
| `C4-sub1` | sub1: 49 y-coefficient equations, minus 1 free scalar = +48 | PASS |
| `C4` | equation counts reproduce TRANSFORM_AUDIT sec.2.4 (33-1=32 / 49-1=48) | PASS |
| `C5` | used-row equation totals 122 (sub2) / 181 (sub1) | PASS |
| `C5b` | the lambda row is a +26 %% / +27 %% enlargement | PASS |
| `D0` | CANONICAL GUARD: G5 = G5body + Phi with coeff(G5, Phi) == 1 | PASS |
| `D1` | FRESH witness on V(G1,G2,G3,G5) with G4 != 0, built by SOLVING the used rows (d0<-G2, d1<-G3, Phi<-G5), not by search | PASS |
| `D1b` | the 4-parameter family (e, R, s, d2) lies on the used variety identically, and G4 is a NON-CONSTANT function on it | PASS |
| `D2` | TRANSFORM_AUDIT witness satisfies all four used rows identically in Phi | PASS |
| `D2b` | and there G4 = -3*Phi + 37/24 != 0 | PASS |
| `D3` | 1-parameter family lies on all four used rows for every w | PASS |
| `D3b` | G4 is NON-CONSTANT on it => the used rows pin neither G4 nor its divisor | PASS |
| `E1` | on the collapsed ansatz G1 == 0 identically (it IS the collapse) | PASS |
| `E1b` | sol4 from generators.json equals -R*(s + d2) - d1*e/2 | PASS |
| `E2` | G4\|collapsed = -(3/2)*( R^2*A + e^2*B + e*R*d1*(s - d2) ), A = d0 - (s+d2)^2, B = R + 2*d0*s + d2*s^2 - d1^2/4 | PASS |
| `E3` | eliminating d0 between G4 and G2 leaves a d0-free (sigma-free) row | PASS |
| `E3b` | d0-free row in closed form, p := d2 + 2*s | PASS |
| `E3c` | and it equals -(3/2)*e^2*G4\|collapsed modulo G2 | PASS |
| `E4` | G2\|collapsed == (3/2) * the T1_BRANCH.md T6 relation W^2 = R^4 + d2*e^2*R^2 + d1*e^3*R + d0*e^4, W = e*S - R^2 | PASS |
| `E4b` | T6 restated: W^2 - (R^4 + d2*e^2*R^2 + d1*e^3*R + d0*e^4) == e^2 * (the T6 relation), W = e*S - R^2 = e^2*s - R^2 | PASS |
| `E5` | G4\|collapsed is NOT implied by <G2,G3,G5>\|collapsed: check D1b exhibits the used variety as a 4-parameter rational family (e, R, s, d2) on which G4 is non-constant | PASS |
| `F1-sub2` | sub2: the lambda row on a live ansatz has degree <= 32 and yields 33 coefficient equations | PASS |
| `F2-sub2` | sub2: lam appears in exactly the 29 rows y^4..y^32 (the nonzero coefficients of y^4*(y+1)^28); the y^32 row pins it, leaving 32 net | PASS |
| `F2b-sub2` | sub2 freedom balance on a live ansatz: 122 used-row equations and 54 unknowns become 155 equations and 55 unknowns (+32 net) | PASS |
| `F1-sub1` | sub1: the lambda row on a live ansatz has degree <= 48 and yields 49 coefficient equations | PASS |
| `F2-sub1` | sub1: lam appears in exactly the 29 rows y^4..y^32 (the nonzero coefficients of y^4*(y+1)^28); the y^32 row pins it, leaving 48 net | PASS |
| `F2b-sub1` | sub1 freedom balance on a live ansatz: 176 used-row equations and 75 unknowns become 225 equations and 76 unknowns (+48 net) | PASS |
| `F3` | MONOTONE: V(old rows + lambda row) projects INTO V(old rows), so an EMPTY cell can never be resurrected by adding the row | PASS |
| `G1` | EVERY monomial of G4 contains dm2, dm3 or dm4 | PASS |
| `G1b` | hence lam = 0 with dm2 = dm3 = dm4 = 0 satisfies the divisor condition on EVERY (d2,d1,d0,dm1): no cell/state test built on the RAW lambda row alone can ever kill | PASS |
| `G2` | on the COLLAPSED ansatz, R = s = 0 gives G4 = (3/8)*d1^2*e^2, nonzero on T1 -- so the trivial point is T2-only there | PASS |
| `G3` | horn 1 (a <= 10): v_t >= 2*a; the row needs v_t = 28 EXACTLY when lam != 0, so lam != 0 requires a <= 14 -- VACUOUS in the standard regime | PASS |
| `G3b` | horn 2 (a >= 11 even): v_t >= 2*rho = 30 - a, so lam != 0 requires a >= 2 -- VACUOUS in the alternate regime too | PASS |
| `G3c` | AND the order floor can never kill even when it is violated: v_t(G4_str) > 28 forces lam = 0, i.e. G4_str == 0 -- a case of the divisor condition, NOT a contradiction. The order calculus can only ever eliminate the lam != 0 BRANCH. | PASS |
| `G4` | marked-root test: v_beta(G4\|collapsed) >= min(2*v_beta(R), 2*b_j, b_j + v_beta(R)) and the row forces 0, so lam != 0 requires v_beta(R) = 0 -- ALREADY committed (ALT_FRONTIER_V2 sec.6, trichotomy at beta). Not new content. | PASS |
| `G5` | no degree-forcing kill: every monomial carries a SPARE, whose stripped degree is capped above but free below, so the row can always be driven to degree < 32 (indeed to 0). deg-counting of the divisor_filter species is structurally inapplicable. | PASS |
| `H1` | sub1 / C08+C20 ON survivor baseline reproduces FRONTIER_REBUILD.md (34 cells / 314 flag cases / 7275 states) | PASS |
| `H1b` | sub1 / C08+C20 OFF baseline reproduces (34 / 322 / 8889) | PASS |
| `H2` | standard sub2 survivor baseline is EMPTY (the regression target) | PASS |
| `H3-sub1-ON` | sub1 / C08+C20 ON: lambda row kills 0 cells, 0 flag cases, 0 states | PASS |
| `H3-sub1-OFF` | sub1 / C08+C20 OFF: lambda row kills 0 cells, 0 flag cases, 0 states | PASS |
| `H3-sub2-ON` | sub2 / C08+C20 ON: lambda row kills 0 cells, 0 flag cases, 0 states | PASS |
| `H3-sub2-OFF` | sub2 / C08+C20 OFF: lambda row kills 0 cells, 0 flag cases, 0 states | PASS |
| `H4` | parsed the 6 surviving alternate-regime T1 branches from ALT_FRONTIER_V2.md sec.6, 562 states total | PASS |
| `H5` | alternate regime: lambda row kills 0 of 6 branches, 0 of 562 states | PASS |

**68 / 68 checks pass.**


```
python -u g4_row.py           # this report
python -u g4_row.py --quiet   # exit 0 iff every check passes
python -u g4_row.py --probe   # + one bounded Singular elimination probe
```

